import { View } from "@tarojs/components";
import * as React from "react";
import isFunction from "lodash/isFunction";
import { prefixClassname } from "../styles/index.js";
import CalendarWeekdays from "./calendar-weekdays.js";
function CalendarHeader(props) {
    const { title, subtitle, showSubtitle, date } = props;
    return (React.createElement(View, { className: prefixClassname("calendar__header") },
        React.createElement(View, { className: prefixClassname("calendar__header-title") }, title),
        showSubtitle && (React.createElement(View, { className: prefixClassname("calendar__header-subtitle") }, isFunction(subtitle) ? (date ? subtitle(date) : "") : subtitle)),
        React.createElement(CalendarWeekdays, null)));
}
export default CalendarHeader;
