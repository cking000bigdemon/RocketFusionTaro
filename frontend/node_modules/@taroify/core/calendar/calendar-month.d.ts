import * as React from "react";
import { type ReactNode } from "react";
import { type CalendarDayObject } from "./calendar.shared";
export interface CalendarMonthInstance {
    disabledDays: CalendarDayObject[];
    getValue(): Date;
    getHeight(): number;
    getRectTop(): Promise<number>;
}
interface CalendarMonthProps {
    value?: Date;
    showMonthTitle?: boolean;
    readonly?: boolean;
    watermark?: boolean;
    children?: ReactNode;
}
declare const CalendarMonth: React.ForwardRefExoticComponent<CalendarMonthProps & React.RefAttributes<CalendarMonthInstance>>;
export default CalendarMonth;
