import { View } from "@tarojs/components";
import _map from "lodash/map";
import * as React from "react";
import { useContext } from "react";
import { prefixClassname } from "../styles/index.js";
import CalendarContext from "./calendar.context.js";
function CalendarWeekdays() {
    const { firstDayOfWeek } = useContext(CalendarContext);
    const weekdays = ["日", "一", "二", "三", "四", "五", "六"];
    const renderWeekdays = [
        ...weekdays.slice(firstDayOfWeek, 7),
        ...weekdays.slice(0, firstDayOfWeek),
    ];
    return (React.createElement(View, { className: prefixClassname("calendar__weekdays") }, _map(renderWeekdays, (day) => (React.createElement(View, { className: prefixClassname("calendar__weekday"), key: day, children: day })))));
}
export default CalendarWeekdays;
