import { createContext } from "react";
import { MAX_DATE, MIN_DATE, } from "./calendar.shared.js";
const CalendarContext = createContext({
    type: "single",
    firstDayOfWeek: 0,
    min: MIN_DATE,
    max: MAX_DATE,
});
export default CalendarContext;
