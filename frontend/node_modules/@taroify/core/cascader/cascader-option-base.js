import { Success } from "@taroify/icons/index.js";
import { View } from "@tarojs/components";
import classNames from "classnames";
import * as React from "react";
import { prefixClassname } from "../styles/index.js";
function CascaderOptionBase(props) {
    const { className, active, disabled, children, ...restProps } = props;
    return (React.createElement(View, { className: classNames(prefixClassname("cascader__option"), {
            [prefixClassname("cascader__option--active")]: active,
            [prefixClassname("cascader__option--disabled")]: disabled,
        }, className), hoverClass: prefixClassname("cascader__option--hover"), ...restProps },
        React.createElement(View, null, children),
        active && React.createElement(Success, { className: prefixClassname("cascader__active-icon") })));
}
export default CascaderOptionBase;
