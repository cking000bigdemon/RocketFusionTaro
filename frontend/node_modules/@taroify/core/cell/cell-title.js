import { View } from "@tarojs/components";
import classNames from "classnames";
import * as React from "react";
import { prefixClassname } from "../styles/index.js";
function CellTitle(props) {
    const { className, titleStyle, titleClass, ...restProps } = props;
    return (React.createElement(View, { className: classNames(prefixClassname("cell__title"), className, titleClass), style: titleStyle, ...restProps }));
}
export default CellTitle;
