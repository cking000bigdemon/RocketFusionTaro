import { View } from "@tarojs/components";
import classNames from "classnames";
import * as React from "react";
import { prefixClassname } from "../styles/index.js";
function CellValue(props) {
    const { className, alone, valueClass, ...restProps } = props;
    return (React.createElement(View, { className: classNames(prefixClassname("cell__value"), {
            [prefixClassname("cell__value--alone")]: alone,
        }, className, valueClass), ...restProps }));
}
export default CellValue;
