import { useUncontrolled } from "@taroify/hooks/index.js";
import { View } from "@tarojs/components";
import classNames from "classnames";
import * as React from "react";
import { prefixClassname } from "../styles/index.js";
import CheckboxGroupContext from "./checkbox-group.context.js";
function CheckboxGroup(props) {
    const { defaultValue, value: valueProp, disabled, max, direction = "vertical", children, onChange: onChangeProp, ...restProps } = props;
    const { value, setValue } = useUncontrolled({
        value: valueProp,
        defaultValue,
        onChange: onChangeProp,
    });
    return (React.createElement(CheckboxGroupContext.Provider, { value: {
            value,
            max,
            disabled,
            direction,
            onChange: setValue,
        } },
        React.createElement(View, { className: classNames(prefixClassname("checkbox-group"), {
                [prefixClassname("checkbox-group--horizontal")]: direction === "horizontal",
                [prefixClassname("checkbox-group--vertical")]: direction === "vertical",
            }), children: children, ...restProps })));
}
export default CheckboxGroup;
