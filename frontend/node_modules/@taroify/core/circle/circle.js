import { View } from "@tarojs/components";
import * as React from "react";
import { prefixClassname } from "../styles/index.js";
import { addUnitPx } from "../utils/format/unit.js";
import CircleCanvas from "./circle-canvas.js";
function Circle({ size = 100, children, ...props }) {
    return (React.createElement(View, { className: prefixClassname("circle"), style: { width: addUnitPx(size), height: addUnitPx(size) } },
        React.createElement(CircleCanvas, { size: size, ...props }),
        children && React.createElement(View, { className: prefixClassname("circle__text"), children: children })));
}
export default Circle;
