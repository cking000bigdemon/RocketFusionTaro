import type { ViewProps } from "@tarojs/components/types/View";
import * as React from "react";
import { type ReactNode } from "react";
import { type CountdownInstance, type CurrentTime } from "./countdown.shared";
import { type UseCountdownOptions } from "./use-countdown";
interface CountdownProps extends Omit<ViewProps, "children">, UseCountdownOptions {
    format?: string | ((current: CurrentTime) => ReactNode);
    children?: ReactNode | ((current: CurrentTime) => ReactNode);
}
declare const Countdown: React.ForwardRefExoticComponent<Omit<CountdownProps, "ref"> & React.RefAttributes<CountdownInstance>>;
export default Countdown;
