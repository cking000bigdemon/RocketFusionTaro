import type { ReactNode } from "react";
interface DialogHeaderProps {
    className?: string;
    children?: ReactNode;
}
export default function DialogHeader(props: DialogHeaderProps): JSX.Element;
export {};
