import { View } from "@tarojs/components";
import classNames from "classnames";
import * as React from "react";
import { default as TaroImage } from "../image/index.js";
import { prefixClassname } from "../styles/index.js";
const PRESET_IMAGES = ["default", "error", "search", "network"];
function obtainImageUrl(image) {
    if (image && PRESET_IMAGES.includes(image)) {
        return `https://img.yzcdn.cn/vant/empty-image-${image}.png`;
    }
    return image;
}
function Empty(props) {
    const { className, ...restProps } = props;
    return React.createElement(View, { className: classNames(prefixClassname("empty"), className), ...restProps });
}
(function (Empty) {
    function Image(props) {
        const { className, style, src = "default" } = props;
        const __src__ = obtainImageUrl(src);
        return (React.createElement(TaroImage, { className: classNames(prefixClassname("empty__image"), className), style: style, src: __src__ }));
    }
    Empty.Image = Image;
    function Description(props) {
        const { className, ...restProps } = props;
        return (React.createElement(View, { className: classNames(prefixClassname("empty__description"), className), ...restProps }));
    }
    Empty.Description = Description;
})(Empty || (Empty = {}));
export default Empty;
