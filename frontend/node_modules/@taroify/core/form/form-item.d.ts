import * as React from "react";
import { type ReactNode } from "react";
import { type CellProps } from "../cell";
import type { FormRule } from "./form.shared";
export interface FormItemProps extends Omit<CellProps, "children"> {
    name?: string;
    defaultValue?: any;
    required?: boolean;
    rules?: FormRule[];
    dependencies?: string[];
    shouldUpdate?: boolean | ((prev: any, next: any) => boolean);
    noStyle?: boolean;
    disabled?: boolean;
    validateFirst?: boolean;
    children?: (() => ReactNode) | ReactNode;
}
declare const _default: React.ForwardRefExoticComponent<FormItemProps>;
export default _default;
