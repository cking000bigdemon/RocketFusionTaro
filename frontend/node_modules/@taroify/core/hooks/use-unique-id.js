import _uniqueId from "lodash/uniqueId";
import { useRef } from "react";
function useUniqueId(prefix = "t_") {
    const uniqueIdRef = useRef(_uniqueId(prefix));
    return uniqueIdRef.current;
}
export default useUniqueId;
