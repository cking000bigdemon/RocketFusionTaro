import { ArrowLeft } from "@taroify/icons/index.js";
import { cloneIconElement } from "@taroify/icons/utils/index.js";
import { View } from "@tarojs/components";
import classNames from "classnames";
import * as React from "react";
import { isValidElement } from "react";
import { prefixClassname } from "../styles/index.js";
export function NavBarLeft(props) {
    const { className, icon = React.createElement(ArrowLeft, null), children, ...restProps } = props;
    return (React.createElement(View, { className: classNames(prefixClassname("navbar__left"), className), ...restProps },
        icon && cloneIconElement(icon, { className: prefixClassname("navbar__icon") }),
        isValidElement(children) ? (children) : (React.createElement(View, { className: prefixClassname("navbar__text"), children: children }))));
}
export default NavBarLeft;
