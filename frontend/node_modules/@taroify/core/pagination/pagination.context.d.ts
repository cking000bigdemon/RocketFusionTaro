import type { Page } from "./pagination.shared";
interface PaginationContextValue {
    current: number;
    prevText: string;
    nextText: string;
    count: number;
    siblingCount: number;
    emitClick?: (page: Page) => void;
}
declare const PaginationContext: import("react").Context<PaginationContextValue>;
export default PaginationContext;
