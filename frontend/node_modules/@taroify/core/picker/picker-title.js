import { View } from "@tarojs/components";
import * as React from "react";
import { prefixClassname } from "../styles/index.js";
export default function PickerTitle(props) {
    const { children } = props;
    return React.createElement(View, { className: prefixClassname("picker__title"), children: children });
}
