import { View } from "@tarojs/components";
import classNames from "classnames";
import * as React from "react";
import { useHeight } from "../hooks/index.js";
import { addUnitPx } from "../utils/format/unit.js";
export default function usePlaceholder(contentRef, { className } = {}) {
    console.warn("[Taroify] usePlaceholder is deprecated, please don't use it.");
    return ({ className: classNameProp, style = {}, children, ...restProps }) => {
        const height = useHeight(contentRef, [children]);
        return (React.createElement(View, { className: classNames(className, classNameProp), style: {
                ...style,
                height: height ? `${addUnitPx(height)}` : "",
            }, children: children, ...restProps }));
    };
}
