import _isFunction from "lodash/isFunction";
import * as React from "react";
import { useContext } from "react";
import PullRefreshContext from "./pull-refresh.context.js";
export function PullRefreshPulling(props) {
    const { children } = props;
    const { distance } = useContext(PullRefreshContext);
    if (_isFunction(children)) {
        return React.createElement(React.Fragment, null, children?.({ distance }));
    }
    return React.createElement(React.Fragment, null, children);
}
export function PullRefreshLoosing(props) {
    const { children } = props;
    return React.createElement(React.Fragment, null, children);
}
export function PullRefreshLoading(props) {
    const { children } = props;
    return React.createElement(React.Fragment, null, children);
}
export function PullRefreshCompleted(props) {
    const { children } = props;
    return React.createElement(React.Fragment, null, children);
}
