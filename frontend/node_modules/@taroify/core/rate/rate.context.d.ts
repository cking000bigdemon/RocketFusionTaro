import { type ReactNode } from "react";
interface RateContextValue {
    gutter?: number;
    count?: number;
    icon?: ReactNode;
    emptyIcon?: ReactNode;
}
declare const RateContext: import("react").Context<RateContextValue>;
export default RateContext;
