import { View } from "@tarojs/components";
import classNames from "classnames";
import * as React from "react";
import { useContext } from "react";
import { prefixClassname } from "../styles/index.js";
import SearchContext from "./search.context.js";
function SearchAction(props) {
    const { className, children = "取消", onClick, ...restProps } = props;
    const { onCancel } = useContext(SearchContext);
    return (React.createElement(View, { className: classNames(prefixClassname("search__action"), className), children: children, onClick: (e) => {
            onClick?.(e);
            onCancel?.(e);
        }, ...restProps }));
}
export default SearchAction;
