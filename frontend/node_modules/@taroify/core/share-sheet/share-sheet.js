import classNames from "classnames";
import * as React from "react";
import Sheet from "../sheet/index.js";
import { prefixClassname } from "../styles/index.js";
import ShareSheetContext from "./share-sheet.context.js";
function ShareSheet(props) {
    const { className, onSelect, ...restProps } = props;
    return (React.createElement(ShareSheetContext.Provider, { value: { onSelect } },
        React.createElement(Sheet, { className: classNames(prefixClassname("share-sheet"), className), ...restProps })));
}
export default ShareSheet;
