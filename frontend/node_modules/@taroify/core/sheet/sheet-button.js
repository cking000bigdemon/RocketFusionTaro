import classNames from "classnames";
import * as React from "react";
import { useContext } from "react";
import { prefixClassname } from "../styles/index.js";
import SheetGap from "./sheet-gap.js";
import SheetItem from "./sheet-item.js";
import SheetContext from "./sheet.context.js";
export default function SheetButton(props) {
    const { className, type, onClick, ...restProps } = props;
    const { onCancel } = useContext(SheetContext);
    function handleClick(event) {
        onClick?.(event);
        if (type === "cancel") {
            onCancel?.();
        }
    }
    return (React.createElement(React.Fragment, null,
        React.createElement(SheetGap, null),
        React.createElement(SheetItem, { className: classNames(prefixClassname("sheet__button"), {
                [prefixClassname("sheet__button--cancel")]: type === "cancel",
            }, className), onClick: handleClick, ...restProps })));
}
