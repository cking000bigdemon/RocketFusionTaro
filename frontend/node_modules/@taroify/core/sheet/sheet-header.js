import { View } from "@tarojs/components";
import classNames from "classnames";
import * as React from "react";
import { prefixClassname } from "../styles/index.js";
export default function SheetHeader(props) {
    const { className, title, children, ...restProps } = props;
    return (React.createElement(View, { className: classNames(prefixClassname("sheet__header"), className), ...restProps },
        title && React.createElement(View, { className: classNames(prefixClassname("sheet__title")), children: title }),
        children && (React.createElement(View, { className: classNames(prefixClassname("sheet__description")), children: children }))));
}
