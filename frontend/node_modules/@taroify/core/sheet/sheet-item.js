import { View } from "@tarojs/components";
import classNames from "classnames";
import * as React from "react";
import Loading from "../loading/index.js";
import { prefixClassname } from "../styles/index.js";
export default function SheetItem(props) {
    const { className, loading, disabled, children, ...restProps } = props;
    return (React.createElement(View, { className: classNames(prefixClassname("sheet__item"), {
            [prefixClassname("sheet__item--loading")]: loading,
            [prefixClassname("sheet__item--disabled")]: disabled,
        }, className), children: loading ? React.createElement(Loading, null) : children, ...restProps }));
}
