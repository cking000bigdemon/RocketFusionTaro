import * as React from "react";
import { View } from "@tarojs/components";
import classNames from "classnames";
import { prefixClassname } from "../styles/index.js";
import { getSizeStyle } from "../utils/format/unit.js";
function SkeletonAvatar(props) {
    const { avatarSize, avatarShape } = props;
    return (React.createElement(View, { className: classNames(prefixClassname("skeleton__avatar"), {
            [prefixClassname("skeleton__avatar--round")]: avatarShape === "round",
        }), style: {
            ...getSizeStyle(avatarSize),
        } }));
}
export default SkeletonAvatar;
