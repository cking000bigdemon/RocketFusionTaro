import * as React from "react";
import { View } from "@tarojs/components";
import { Icon } from "@taroify/icons/index.js";
import classNames from "classnames";
import { prefixClassname } from "../styles/index.js";
import { getSizeStyle } from "../utils/format/unit.js";
function SkeletonImage(props) {
    const { imageSize, imageShape } = props;
    return (React.createElement(View, { className: classNames(prefixClassname("skeleton__image"), {
            [prefixClassname("skeleton__image--round")]: imageShape === "round",
        }), style: getSizeStyle(imageSize) },
        React.createElement(Icon, { name: "photo", className: prefixClassname("skeleton__image-icon") })));
}
export default SkeletonImage;
