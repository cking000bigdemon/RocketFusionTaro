import * as React from "react";
import { View } from "@tarojs/components";
import classNames from "classnames";
import { prefixClassname } from "../styles/index.js";
import { addUnitPx } from "../utils/format/unit.js";
function SkeletonTitle(props) {
    const { round, titleWidth } = props;
    return (React.createElement(View, { className: classNames(prefixClassname("skeleton__title"), {
            [prefixClassname("skeleton__title--round")]: round,
        }), style: { width: addUnitPx(titleWidth) } }));
}
export default SkeletonTitle;
