import { View } from "@tarojs/components";
import classNames from "classnames";
import * as React from "react";
import { forwardRef } from "react";
import { prefixClassname } from "../styles/index.js";
const SwipeCellActions = forwardRef((props, ref) => {
    const { className, side, ...restProps } = props;
    return (React.createElement(View, { ref: ref, className: classNames(prefixClassname("swipe-cell__actions"), {
            [prefixClassname("swipe-cell__left")]: side === "left",
            [prefixClassname("swipe-cell__right")]: side === "right",
        }, className), ...restProps }));
});
export default SwipeCellActions;
