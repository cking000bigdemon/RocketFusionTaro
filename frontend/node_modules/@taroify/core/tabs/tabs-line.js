import { View } from "@tarojs/components";
import classNames from "classnames";
import * as React from "react";
import { useContext } from "react";
import { prefixClassname } from "../styles/index.js";
import TabsContext from "./tabs.context.js";
export default function TabsLine(props) {
    const { active } = props;
    const { duration } = useContext(TabsContext);
    return (React.createElement(View, { className: classNames(prefixClassname("tabs__line"), {
            [prefixClassname("tabs__line--active")]: active,
        }), style: { transitionDuration: `${duration}ms` } }));
}
