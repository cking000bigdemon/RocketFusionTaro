import type { ViewProps } from "@tarojs/components/types/View";
import { type PropsWithChildren, type ReactNode } from "react";
import { type TimelineDotProps } from "./timeline-dot";
export interface TimelineItemProps extends PropsWithChildren<ViewProps> {
    dot?: ReactNode | TimelineDotProps;
}
declare function TimelineItem(props: TimelineItemProps): JSX.Element;
export default TimelineItem;
