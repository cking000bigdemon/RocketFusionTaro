import { View } from "@tarojs/components";
import classNames from "classnames";
import * as React from "react";
import { prefixClassname } from "../styles/index.js";
import TimelineContext from "./timeline.context.js";
function Timeline(props) {
    const { className, position, ...restProps } = props;
    return (React.createElement(TimelineContext.Provider, { value: {
            position,
        } },
        React.createElement(View, { className: classNames(prefixClassname("timeline"), className), ...restProps })));
}
export default Timeline;
