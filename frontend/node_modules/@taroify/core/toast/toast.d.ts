import type { ViewProps } from "@tarojs/components/types/View";
import { type CSSProperties, type ReactNode } from "react";
import { type ToastPosition, type ToastType } from "./toast.shared";
export interface ToastProps extends ViewProps {
    className?: string;
    style?: CSSProperties;
    defaultOpen?: boolean;
    open?: boolean;
    type?: ToastType;
    position?: ToastPosition;
    icon?: ReactNode;
    duration?: number;
    children?: ReactNode;
    onClose?(opened: boolean): void;
}
export default function Toast(props: ToastProps): JSX.Element;
