import classNames from "classnames";
import * as React from "react";
import SidebarTab from "../sidebar/sidebar-tab.js";
import { prefixClassname } from "../styles/index.js";
function TreeSelectTab(props) {
    const { className, title, children, ...restProps } = props;
    return (React.createElement(SidebarTab, { className: classNames(prefixClassname("tree-select-tab"), className), children: title, ...restProps }));
}
export default TreeSelectTab;
