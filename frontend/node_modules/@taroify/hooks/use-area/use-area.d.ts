import { type AreaData, type AreaDivision } from "./area.shared";
export type AreaFormatter = (record?: Record<string, string>, prefix?: any) => AreaDivision[];
interface UseAreaOptions {
    depth?: number;
    data?: AreaData;
    formatter?: AreaFormatter;
}
export default function useArea(unverifiedValue?: any[], options?: UseAreaOptions): {
    columns: AreaDivision[];
    values: string[];
    valueOptions: AreaDivision[];
    getValues: () => string[];
    getValueOptions: () => AreaDivision[];
    setValues: (newValues: any[]) => void;
};
export {};
