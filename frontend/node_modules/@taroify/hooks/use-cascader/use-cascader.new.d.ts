import { type CascaderOption } from "./use-cascader.shared";
export interface UseCascaderNewOptions {
    value?: any[];
    depth?: number;
    data?: CascaderOption[];
    onChange?(value: any[]): void;
}
export interface CascaderObjectNew {
    columns: CascaderOption[];
}
export default function useCascaderNew({ value: values, depth: maxDepth, data, onChange, }: UseCascaderNewOptions): CascaderObjectNew;
