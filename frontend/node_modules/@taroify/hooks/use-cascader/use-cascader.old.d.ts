import type { CascaderOption } from "./use-cascader.shared";
interface CascaderFieldNames {
    label?: string;
    value?: string;
    children?: string;
}
export interface UseCascaderOldOptions {
    value?: any[];
    depth?: number;
    options?: CascaderOption[];
    fieldNames?: CascaderFieldNames;
    refreshKey?: number;
}
export interface CascaderObjectOld {
    columns: CascaderOption[][];
}
export default function useCascaderOld({ value: values, depth, options, fieldNames, refreshKey, }: UseCascaderOldOptions): CascaderObjectOld;
export {};
