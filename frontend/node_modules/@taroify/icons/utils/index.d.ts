import { type ReactNode } from "react";
import { type IconProps } from "../shared";
export declare function isIconElement(node?: ReactNode): boolean;
export declare function cloneIconElement(node: ReactNode, props: IconProps): ReactNode;
