import type { ViewProps } from "@tarojs/components/types/View";
import type { CSSProperties, ReactNode } from "react";
import { type IconColor, type IconProps, type IconSize } from "../shared";
interface VanIconProps extends ViewProps {
    className?: string;
    style?: CSSProperties;
    name?: string;
    size?: IconSize | number | string;
    classPrefix?: string;
    color?: IconColor | string;
    children?: ReactNode;
}
export default function VanIcon({ className, style, name, size, classPrefix, color, ...restProps }: VanIconProps): JSX.Element;
export declare function createVanIconComponent(name: string): (props: IconProps) => JSX.Element;
export {};
